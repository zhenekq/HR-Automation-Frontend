import { CombinedSearchFilterPipe } from './combined-search-filter.pipe';

describe('CombinedSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CombinedSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
